services:
  doorbell-app:
    build: .
    container_name: doorbell-app
    restart: unless-stopped
    ports:
      - "8088:8088"
    volumes:
      - ./app/data/video:/data/video
      - ./app/data/tts:/data/tts
    env_file:
      - .env
    environment:
      VIDEO_DIR: /data/video
      TTS_DIR: /data/tts
      TTS_VOICE: de-DE-SeraphinaMultilingualNeural
      TTS_RATE: +0%

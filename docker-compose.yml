services:
  doorbell-app:
    build: .
    container_name: doorbell-app
    restart: unless-stopped
    ports:
      - "8088:8088"
    volumes:
      - ./app/data/video:/data/video
    env_file:
      - .env
    environment:
      VIDEO_DIR: /data/video
